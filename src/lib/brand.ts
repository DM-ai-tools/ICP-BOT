/**
 * Product identity, in one place.
 *
 * "Prism": the product takes one beam — a single brief — and splits it into a
 * spectrum of awareness stages, each a different colour of the same buyer.
 * That is literally what the app does, it is one syllable, and it does not
 * contain the word "builder".
 *
 * Change APP_NAME here and it changes everywhere: browser tab, header, exports,
 * admin panel.
 */

export const APP_NAME = 'Prism';

/** Used where the product needs attributing, e.g. document metadata. */
export const APP_OWNER = 'Traffic Radius';
export const APP_FULL_NAME = `${APP_NAME} by ${APP_OWNER}`;

export const APP_TAGLINE = 'One conversation. Every awareness stage.';

export const APP_DESCRIPTION =
  'Describe your business in plain English. Get detailed, downloadable Ideal Customer Profiles across every awareness stage.';

/**
 * Logo asset. Drop the official file at this path to replace the recreation —
 * the header reads whatever is there, so no code change is needed.
 */
export const LOGO_SRC = '/traffic-radius-logo.svg';
